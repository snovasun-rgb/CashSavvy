import React from 'react'
import ReactDOM from 'react-dom/client'
import FinMatePrototype from '../fintech.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FinMatePrototype />
  </React.StrictMode>,
)