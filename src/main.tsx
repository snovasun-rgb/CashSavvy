import React from 'react'
import ReactDOM from 'react-dom/client'
import FinMatePrototype from '../fintech'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FinMatePrototype />
  </React.StrictMode>,
)